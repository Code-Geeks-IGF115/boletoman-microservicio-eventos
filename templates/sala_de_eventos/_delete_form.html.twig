<form method="post" action="{{ path('app_sala_de_eventos_delete', {'id': sala_de_evento.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sala_de_evento.id) }}">
    <button class="btn">Delete</button>
</form>
